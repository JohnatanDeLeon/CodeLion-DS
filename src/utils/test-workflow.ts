/**
 * Utility function added for workflow testing
 * This change should trigger the changesets to actually publish
 */
export const testWorkflow = () => {
  return "Workflow test successful!";
};

export default testWorkflow;
